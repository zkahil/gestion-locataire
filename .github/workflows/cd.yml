
name: CD
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker/setup-buildx-action@v2
      - uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t ${{ secrets.DOCKER_USERNAME }}/gestion-backend:latest ./backend && docker push ${{ secrets.DOCKER_USERNAME }}/gestion-backend:latest
      - run: docker build -t ${{ secrets.DOCKER_USERNAME }}/gestion-web:latest ./frontend-web && docker push ${{ secrets.DOCKER_USERNAME }}/gestion-web:latest
